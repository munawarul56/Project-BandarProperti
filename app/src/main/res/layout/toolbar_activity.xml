<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="toolbarTitle"
            type="String" />
    </data>

    <android.support.v7.widget.Toolbar
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorPrimary">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/toolbar_back"
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:layout_centerVertical="true"
                app:srcCompat="@drawable/ic_left_arrow_dark"
                tools:ignore="OnClick" />

            <com.bandarproperti.customfonts.MyTextView_Roboto_Bold
                android:id="@+id/toolbar_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="30dp"
                android:layout_centerVertical="true"
                android:textSize="@dimen/toolbar_text_size"
                android:textStyle="bold"
                android:textColor="@color/white"
                android:text="@{toolbarTitle}"/>

            <com.like.LikeButton
                android:id="@+id/toolbar_favorit_btn"
                app:icon_type="heart"
                app:icon_size="20dp"
                app:like_drawable="@drawable/ic_love_fill_white"
                app:unlike_drawable="@drawable/ic_love_border_white"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_marginEnd="10dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"/>
        </RelativeLayout>
    </android.support.v7.widget.Toolbar>
</layout>